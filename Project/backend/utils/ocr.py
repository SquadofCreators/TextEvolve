import os

def run_ocr_model(file_path):
    """
    Placeholder function for your ML model (OCR).
    Replace this with actual inference code.
    """
    return f"Extracted text from {os.path.basename(file_path)}"
